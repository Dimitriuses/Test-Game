//import { readdirSync } from 'fs';
//import { readdirSync } from 'node:fs';

//import fs from 'fs';

export function assetsToResources(path: string){
  const fs = require("fs");
  console.log(fs);
  // fs.readdir(path, (err, files) => {
  //   files.forEach(f => { console.log(f); })

  // } )//.forEach((file) => console.log(file));


}